// view.tsx / edit.tsx / message.tsx
import { View, Text } from 'react-native';

export default function EditClient() {
  // EditContact / MessageContact
  return (
    <View className="flex-1 items-center justify-center">
      <Text>Editar contacto</Text>
    </View>
  );
}
